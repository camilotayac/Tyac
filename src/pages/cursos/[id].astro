---
// src/pages/cursos/[id].astro
import LandingPageTemplate from "../../components/LandingPageTemplate.astro";
import { cursosCiencia } from "../../data/cursos.js";

export async function getStaticPaths() {
    return cursosCiencia.map((curso) => ({
        params: { id: curso.id },
        props: { curso },
    }));
}

const { curso } = Astro.props;
---

<LandingPageTemplate curso={curso} tipo="curso" />
